apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aya-node.fullname" . }}-test-connection"
  labels:
    {{- include "aya-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aya-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
